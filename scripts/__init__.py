"""Demo / ops scripts for Aegis Protocol.

These modules orchestrate existing stubs into a captioned demo flow
the judges can run with no external services. They are intentionally
NOT imported from runtime code paths — they exist for demo and docs.
"""
