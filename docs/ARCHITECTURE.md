# Architecture

## Goals

Aegis Protocol is a **self-deployable AI support agent for Web3 communities**. It targets a uniquely-Web3 problem class that generic chatbots (Intercom, Drift, etc.) cannot address: scammers impersonating official support in Discord/Telegram channels and draining user wallets. The system is designed around four hard requirements:

1. **Verifiable identity.** Each agent operates under an ENS subname (`support.<project>.eth`) whose ownership is on-chain and resolvable by any client. Impersonation becomes detectable.
2. **On-chain context.** The agent reads wallet balances, transaction history, and live protocol state, and can simulate suspicious transactions before recommending them. Generic chatbots cannot.
3. **Verifiable inference.** Every model call runs inside a 0G Compute TEE, producing a cryptographic proof that the response was generated by the declared model on the declared input.
4. **Tamper-evident audit trail.** Every reply is logged to 0G DA. Anyone can audit what the agent said, to whom, and when.

## High-level diagram

```
[User in Telegram/Discord]
            ↓
[Channel Adapter] ←─→ [Agent Runtime]
                              ↓
                ┌─────────────┼──────────────┐
                ↓             ↓              ↓
          [0G Storage]  [0G Compute TEE]  [0G DA]
          docs+embed    LLM inference      audit log
                ↓             ↓              ↓
          [Hybrid Search]     ↓              ↓
          BM25+dense+RRF      ↓              ↓
                ↓             ↓              ↓
          [On-chain Tools] ───┘              ↓
          ├─ wallet lookup                   ↓
          ├─ tx simulation                   ↓
          ├─ live protocol state             ↓
          └─ ENS resolver                    ↓
                ↓                            ↓
          [Verified Response] ───────────────┘
                ↓
          [Receipt on 0G Chain]
                ↓
          [Reply via channel → User]
```

## Component map

| Component | Code location | Purpose |
|---|---|---|
| **HTTP API** | `aegis/api/` | REST endpoints, webhooks, admin panel backend |
| **Agent runtime** | `aegis/agent/` | Observe → decide → act loop, tool registry |
| **0G Storage client** | `aegis/storage/zerog.py` | Upload/retrieve documents and embeddings |
| **0G Compute client** | `aegis/compute/zerog.py` | TEE-verified LLM inference |
| **On-chain tools** | `aegis/chain/` | Wallet lookup, tx simulation, ENS resolution, registry contract interface |
| **Channel adapters** | `aegis/channels/` | Telegram (AIOgram) and Discord (discord.py) bots |
| **RAG pipeline** | `aegis/rag/` | Embeddings, hybrid search (BM25 + dense + RRF), reranker (FlashRank) |
| **Database** | `aegis/db/` | PostgreSQL schema for tenants, agents, documents, audit log |
| **Configuration** | `aegis/config.py` | Pydantic Settings, `.env`-driven |

## Multi-tenant data model

Four core tables (Day 2):

- `tenants` — projects that deploy an Aegis agent (e.g. a DAO).
- `agents` — individual agent instances, each with an ENS subname and a knowledge-base CID on 0G Storage.
- `documents` — uploaded documentation, indexed by sha256 + 0G CID.
- `audit_log` — every user query, response, TEE proof, and 0G DA tx hash.

Foreign keys enforce tenant isolation. All vector embeddings live in Qdrant, partitioned by tenant.

## Tech stack

- **Backend:** Python 3.12, FastAPI, async SQLAlchemy 2, asyncpg
- **Storage:** PostgreSQL 16, Redis 7, Qdrant
- **AI/RAG:** FastEmbed (embeddings), BM25 + RRF (hybrid retrieval), FlashRank (reranker)
- **Web3:** 0G Storage / Compute / DA / Chain, ENS (via web3.py), Solidity (registry contract)
- **Channels:** AIOgram, discord.py
- **Tooling:** uv, Ruff, mypy, pytest, pre-commit, GitHub Actions

## Trust boundaries

- **User ↔ Channel:** standard platform trust (Discord/Telegram TLS).
- **Channel ↔ Agent runtime:** internal HTTP, runs inside the same deployment.
- **Agent runtime ↔ 0G Compute:** TEE attestation; the runtime verifies the proof before forwarding the answer.
- **Agent runtime ↔ 0G Storage / DA / Chain:** signed transactions from the agent's wallet.
- **Tenant ↔ Agent:** RBAC via wallet signatures on the admin panel; the agent's ENS subname provides public verifiability.

## On-chain registry trust model

`AegisRegistry.sol` (deployed to the 0G chain) is the public source of truth for "which wallet runs which `support.<project>.eth` agent". The contract is intentionally minimal — no proxy, no upgrade path, no admin keys, no external deps. Trust derives from two things only: (1) the immutable bytecode at the deployed address, and (2) the project's signing key, which is the sole controller of every record it created. A user verifying a Telegram or Discord bot reads `AegisRegistry.get("support.<project>.eth")`, checks that `record.active == true` and that `record.owner` matches the project's known wallet, then optionally cross-checks the knowledge-base CID hash against what the agent claims to be serving. Per `CLAUDE.md` §3, the agent runtime never signs registry transactions automatically: `aegis.chain.registry.AegisRegistry.register` returns an unsigned tx dict by default, and signing is the caller's job.

## Roadmap mapping

| Day | Component delivered |
|---|---|
| 1 | Skeleton, FastAPI `/health`, docker-compose, docs, CI |
| 2 | 0G Storage / Compute clients, multi-tenant DB schema, Alembic migrations |
| 3 | RAG pipeline (embed, hybrid search, rerank) |
| 4 | Agent runtime with tool registry |
| 5 | Telegram + Discord channel adapters |
| 6 | On-chain context tools (wallet lookup, tx simulation, ENS resolver) |
| 7 | ENS subname registration; `AegisRegistry.sol` deployed to 0G chain |
| 8 | KeeperHub scheduled tasks; minimal admin panel; demo-video script |
| 9 | Final polish, demo recording, submission |

## Non-goals (this hackathon)

- DeFi-specific routing (left to integrators)
- Multi-agent orchestration via Gensyn AXL (skipped — too large for solo)
- Custom L2 / appchain (out of scope)
